# 1-4-9 Summary

One-line: Added three fresh roles with networked abilities and native AU cosmetics, plus polished targeting buttons and VFX.

## 4 Key Changes
- Shadow: Blink behind target and Cloak with pulse VFX (RPC-synced).
- Guardian: Shield nearest ally with green outline and pulse VFX (RPC-synced).
- Trickster: Smoke burst that darkens nearby players with pulse VFX (RPC-synced).
- Buttons: Targeting via CustomActionButton<PlayerControl> with proper outlines and cooldowns.

## 6 Details
- RPCs: Host-authoritative MethodRpc in `Networking/AbilityRpcs.cs` (blink, cloak, shield, smoke).
- Cosmetics: Uses `PlayerControl.cosmetics` (phantom alpha, outline, fade black) in `Cosmetics/AbilityCosmetics.cs`.
- Roles: `ShadowRole`, `GuardianRole`, `TricksterRole` with float-backed options and safe getters.
- Targeting: `GetTarget()`, `IsTargetValid()`, `SetOutline(...)` aligned with MiraAPI wiki.
- VFX: `SpawnPulseRing(...)` adds a simple, scalable/fading ring for feedback.
- Cleanup: Removed unavailable attributes, fixed enums/namespaces, and resolved IL2CPP ambiguities.
